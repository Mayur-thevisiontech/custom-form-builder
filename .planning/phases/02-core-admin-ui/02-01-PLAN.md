# Plan: 02-01 - Implement Forms Dashboard and Basic Creator

## Objective
Create the initial UI for managing and creating forms, ensuring the "First Name" and "Email" fields are mandatory.

## Context
We have the Prisma schema ready. Now we need the Remix routes to interact with it.

## Requirements
- **BLD-01**: Admin can create, edit, and delete forms.
- **User Request**: "Firstname & email" cannot be deleted. Provide form name first.

## Tasks

<task wave="1">
  <action>
    Create `app/routes/app.forms.jsx`. This page will list all forms for the current shop.
    Include an EmptyState if no forms exist.
  </action>
  <read_first>
    - app/routes/app._index.jsx (for pattern)
  </read_first>
  <acceptance_criteria>
    - `/app/forms` loads successfully.
    - Displays a "Create Form" button.
  </acceptance_criteria>
</task>

<task wave="2">
  <action>
    Create `app/routes/app.forms.new.jsx`. This page will be the form creator.
    - Field for "Form Name".
    - Initial state for `fields` with First Name and Email.
    - Logic to prevent deletion of these two.
    - Action to save to database.
  </action>
  <read_first>
    - prisma/schema.prisma
  </read_first>
  <acceptance_criteria>
    - `/app/forms/new` loads successfully.
    - First Name and Email fields are visible but have no delete button.
    - Saving redirect back to `/app/forms`.
  </acceptance_criteria>
</task>

<task wave="3">
  <action>
    Implement basic "Delete" functionality in the Dashboard (`app.forms.jsx`).
  </action>
  <acceptance_criteria>
    - Deleting a form removes it from the database and the list.
  </acceptance_criteria>
</task>

## Verification
- Create a form named "Test Form".
- Verify First Name and Email are present.
- Verify Submit button text can be changed.
- Verify form appears in the list.
