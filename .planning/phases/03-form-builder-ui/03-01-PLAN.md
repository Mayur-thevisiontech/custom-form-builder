# Plan: 03-01 - Expand Form Builder Field Types

## Objective
Implement a multi-type field editor in the form builder.

## Tasks

<task wave="1">
  <action>
    Update `app/routes/app.forms.new.jsx` state and UI.
    - Add `type` selector to the field editor.
    - Implement options management (Add/Remove options) for choice types (`checkbox`, `radio`, `select`).
  </action>
  <acceptance_criteria>
    - User can change a field type via a dropdown.
    - When `radio` or `select` is chosen, an "Options" section appears.
  </acceptance_criteria>
</task>

<task wave="2">
  <action>
    Implement specific field types:
    - `textarea` (multi-line)
    - `file` (upload)
    - `phone` (with country hint/dropdown - simplified for now as a type)
  </action>
  <acceptance_criteria>
    - All requested types are available in the dropdown.
  </acceptance_criteria>
</task>

## Verification
- Create a form with one of each type.
- Verify that options for a radio field are saved correctly.
- Verify that `First Name` and `Email` remain non-deletable.
